id: BUG-20250926-1
project: front-store
template: Bug Fix
description: "Storybook fails to start with the error 'Cannot find module ../src/components'"

tasks:
  - name: "Fix Storybook path configuration"
    description: "Investigate and fix the incorrect path or alias configuration in .storybook/main.js that is causing the 'Cannot find module' error."
    files:
      - ".storybook/main.js"
    steps:
      - "Examine the .storybook/main.js file to identify the incorrect path."
      - "Correct the path or add a Webpack alias for the src directory."
      - "Verify that Storybook starts successfully after the fix."
    conditions:
      - follow DRY, KISS
      - no duplicate functions
      - no ghost code
      - no overlaping code
      - confirm if the bug confused
      - no unnecessary complexity

checklist:
  - [ ] Bug reproducible
  - [ ] Fix applied
  - [ ] Tests passed

tracking:
  document: "../.tracking/bug_tracking.md"
  entry: "BUG-20250926-1 | Storybook start has error Failed to build the preview | new"